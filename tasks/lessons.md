# tasks/lessons.md — 项目级 Lesson 记录
# 格式参照全局规则 §6.2
# 每次用户纠正后追加到顶部

- **ID**: L-2026-05-22-001
  - **领域**: 爬虫
  - **适用范围**: 全局
  - **规则**: 校验外部域名的 robots.txt 时，严禁使用原生不带超时的 `urllib.robotparser` 读取。必须使用带超时（如 5 秒）并携带 User-Agent 等 Headers 的 Request Session 请求，成功后再通过 `parse` 进行解析。防反爬严格的站点出现 403/404/超时 等异常时应当安全捕获并降级默认允许，以避免整个管道阻塞挂起。
  - **状态**: 保留在项目内（跳过全局Wiki提升），最后审查 2026-05-22

- **ID**: L-2026-05-22-002
  - **领域**: 前端
  - **适用范围**: 全局
  - **规则**: 在动态 HTML 拼接中，应清退所有内联布局控制与字体字号样式（如 margin-bottom、font-size 等），统一在 style.css 中定义为结构化类选择器，以保障页面响应式和可维护性。
  - **状态**: 保留在项目内（跳过全局Wiki提升），最后审查 2026-05-22

